{
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "main": "backend/dist/src/index.js",
  "scripts": {
    "start": "node backend/dist/index.js",
    "build": "yarn --cwd backend build && yarn --cwd frontend build",
    "heroku-postbuild": "yarn build"
  }
}
